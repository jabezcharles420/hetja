{
  "name": "@straynet/api",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@straynet/contracts": "workspace:*",
    "@straynet/db": "workspace:*",
    "@straynet/ledger": "workspace:^",
    "fastify": "^5.0.0",
    "zod": "^3.23.0"
  }
}
