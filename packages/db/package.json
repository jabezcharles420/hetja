{
  "name": "@hetja/db",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "migrate": "tsx src/migrate.ts",
    "seed": "tsx src/seed.ts",
    "seed:care": "tsx src/seed-care.ts",
    "import:care": "tsx src/import-care-verified.ts",
    "geocode:care": "GEOCODE=1 tsx src/import-care-verified.ts"
  },
  "dependencies": {
    "libphonenumber-js": "^1.13.10",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/pg": "^8.21.0"
  }
}
